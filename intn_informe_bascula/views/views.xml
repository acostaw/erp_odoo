<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="intn_informe_bascula.list">
      <field name="name">intn_informe_bascula list</field>
      <field name="model">intn_informe_bascula.intn_informe_bascula</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="intn_informe_bascula.action_window">
      <field name="name">intn_informe_bascula window</field>
      <field name="res_model">intn_informe_bascula.intn_informe_bascula</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="intn_informe_bascula.action_server">
      <field name="name">intn_informe_bascula server</field>
      <field name="model_id" ref="model_intn_informe_bascula_intn_informe_bascula"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="intn_informe_bascula" id="intn_informe_bascula.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="intn_informe_bascula.menu_1" parent="intn_informe_bascula.menu_root"/>
    <menuitem name="Menu 2" id="intn_informe_bascula.menu_2" parent="intn_informe_bascula.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="intn_informe_bascula.menu_1_list" parent="intn_informe_bascula.menu_1"
              action="intn_informe_bascula.action_window"/>
    <menuitem name="Server to list" id="intn_informe_bascula" parent="intn_informe_bascula.menu_2"
              action="intn_informe_bascula.action_server"/>
    -->
  </data>
</odoo>