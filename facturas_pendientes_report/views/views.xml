<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="facturas_pendientes_report.list">
      <field name="name">facturas_pendientes_report list</field>
      <field name="model">facturas_pendientes_report.facturas_pendientes_report</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="facturas_pendientes_report.action_window">
      <field name="name">facturas_pendientes_report window</field>
      <field name="res_model">facturas_pendientes_report.facturas_pendientes_report</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="facturas_pendientes_report.action_server">
      <field name="name">facturas_pendientes_report server</field>
      <field name="model_id" ref="model_facturas_pendientes_report_facturas_pendientes_report"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="facturas_pendientes_report" id="facturas_pendientes_report.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="facturas_pendientes_report.menu_1" parent="facturas_pendientes_report.menu_root"/>
    <menuitem name="Menu 2" id="facturas_pendientes_report.menu_2" parent="facturas_pendientes_report.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="facturas_pendientes_report.menu_1_list" parent="facturas_pendientes_report.menu_1"
              action="facturas_pendientes_report.action_window"/>
    <menuitem name="Server to list" id="facturas_pendientes_report" parent="facturas_pendientes_report.menu_2"
              action="facturas_pendientes_report.action_server"/>
    -->
  </data>
</odoo>